{
  "name": "@repo/neon-postgres",
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "db:push": "prisma db push",
    "db:seed": "tsx src/seed.ts",
    "db:generate": "prisma generate",
    "db:reset": "prisma db push --force-reset"
  },
  "devDependencies": {
    "prisma": "^6.3.1"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1"
  }
}
